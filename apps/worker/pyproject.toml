[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "careerfit-worker"
version = "0.0.0"
description = "Lightweight CareerFit worker service"
requires-python = ">=3.11"

