# Story 1.1: Register an account — Tasks

Return to the [story overview](README.md). Story lifecycle comes from `sprint-status.yaml`; task lifecycle is maintained only in this file.

## Tasks & Acceptance

**Execution:**

- [x] TASK-1-1-01: Freeze registration and User contract fixtures
  - Status: `done`
  - Owner: `Codex`
  - Branch/worktree: `feat/registration-user-contracts`
  - Depends on: `none`
  - Covers: `AC-1-1-register-an-account-01`, `AC-1-1-register-an-account-02`, `AC-1-1-register-an-account-03`, `AC-1-1-register-an-account-04`, `AC-1-1-register-an-account-05`
  - Scope: 01. Resolve the approved subset of `E1-DEC-001`, `E1-DEC-002`, `E1-DEC-007`, and `E1-DEC-009` into one versioned registration/current-account contract; 02. promote the approved stable contract under `docs/contracts/` without duplicating the global HTTP/error standards; 03. add one executable fixture corpus consumed by both Laravel and Vue verification; 04. map every fixture row to the canonical ACs and the shared IDs below. The fixture boundary must cover CSRF/session bootstrap and expiry, valid guest registration, duplicate and concurrent duplicate email, every invalid/missing/password-policy input class, authenticated-caller registration, throttling, malformed transport, lost-response reconciliation, current-account success/unauthenticated failure, status/error/details paths, private/no-store response behavior, and credential/redaction assertions. Do not freeze unresolved values by assumption.
  - Source boundary: planning inputs are `docs/contracts/common/http.md`, `docs/standards/{api,errors,security,validation,testing}.md`, the Epic 1 package, and the source consumers in `apps/api/{app,bootstrap,config,routes,database,tests}` and `apps/web/src/{features/auth,shared/api,app/router,pages/auth}`. The current scaffold mismatch is evidence for the fixture: web auth currently calls `/auth/*`, expects a bearer `token`, stores it in Pinia, and the API exposes only `/api/health`; the target contract is `/api/v1`, Sanctum stateful HttpOnly cookies, CSRF, and `data.user` without credential material. The approved fixture paths are `docs/contracts/auth/registration.md` and `docs/contracts/auth/fixtures/registration-v1.json`. PostgreSQL 16 is the canonical integration datastore; the fixture itself remains database-neutral.
  - Coordination: `E1-COORD-AUTH-001`
  - Blocked by: none for planning; implementation start still requires the reserved `E1-COORD-AUTH-001` branch/worktree and the user-delegated Codex owner to be explicitly recorded.
  - Outcome: 01. Add executable auth/User contract fixtures: one approved contract document plus one versioned fixture corpus is the sole cross-layer source for registration and current-account request/response/error examples; fixture rows contain no real credentials, hashes, cookies, CSRF tokens, or bearer tokens.
  - Acceptance: 01. Add executable auth/User contract fixtures: every approved auth matrix row is represented exactly once (or has an explicit reusable fixture reference), each row names its AC/Global/Epic/Decision references, the public User projection excludes `password`, `password_hash`, `remember_token`, session/CSRF material and internal security metadata, and no frontend/backend consumer needs a local contract variant. The task remains blocked when any required decision is `pending`.
  - Verification: 01. Add executable auth/User contract fixtures: `jq empty docs/contracts/auth/fixtures/registration-v1.json` passed; jq consumer assertions passed for the public User keys, 13 unique fixture IDs, non-empty AC mappings, required operation/request/expect fields, and the absence of forbidden credential fields from the public projection; `git diff --check` passed. Manual review confirms the stable `/api/v1` envelope, status/code/details/header cases and source-to-target mismatch against the global standards. Evidence paths: `docs/contracts/auth/registration.md` and `docs/contracts/auth/fixtures/registration-v1.json`.

- [x] TASK-1-1-02: Deliver registration and account backend
  - Status: `done`
  - Owner: `codex`
  - Branch/worktree: `feat/registration-account-bebe`
  - Depends on: `TASK-1-1-01`
  - Covers: `AC-1-1-register-an-account-01`, `AC-1-1-register-an-account-02`, `AC-1-1-register-an-account-03`, `AC-1-1-register-an-account-04`, `AC-1-1-register-an-account-05`
  - Scope: 01. Configure Sanctum SPA authentication: add the approved Sanctum/API installation, stateful-domain and credentialed-CORS configuration, CSRF bootstrap, host-only session cookie attributes, session regeneration/invalidation, and JSON rendering for `/api/v1` framework failures. | 02. Implement registration identity and persistence: add the bounded User registration use case/application DTO, canonical name/email/password rules, transaction boundary through the existing `TransactionManager`, and concrete Eloquent persistence using the existing bigint `users.id`, unique email index, and `User` password cast; do not add a generic repository abstraction without a concrete consumer. | 03. Expose register/current-account operations: add Form Requests, thin controllers, Public User API Resource, `/api/v1/auth/register`, `/api/v1/auth/me`, `auth:sanctum` protection, and common error mapping for malformed JSON, validation, duplicate race, authenticated caller, expired session/CSRF, throttling, and unexpected failures. | 04. Enforce abuse/privacy: add separately hashed trusted-IP and canonical-email limiter keys with the approved Redis windows, `Retry-After`, no credential logging, no User disclosure, private/no-store responses, and no bearer-token path. | 05. Verify backend contract: PHPUnit unit/feature tests plus declared disposable-database integration tests consuming `docs/contracts/auth/fixtures/registration-v1.json`; cover transaction rollback, unique/concurrent duplicate, session regeneration, CSRF/expiry, throttling, redaction, and current-account ownership.
  - Source boundary: current API is only `GET /api/health`; `composer.json` has no Sanctum; `bootstrap/app.php` has no middleware or API exception envelope; `User` already has fillable `name/email/password`, hidden `password/remember_token`, and a `hashed` password cast; the initial migration already has a unique `users.email` index and database `sessions` table; PHPUnit currently forces SQLite while the canonical integration target is PostgreSQL 16. The implementation must consume the v1 contract/fixture and must not copy the current web bearer-token scaffold.
  - Coordination: `E1-COORD-AUTH-001`; one backend integration owner coordinates auth/session setup, User persistence, API/error mapping, and the complete backend evidence set after `TASK-1-1-01`.
  - Blocked by: approved `TASK-1-1-01` fixture checkpoint and assignment of one backend integration owner and branch/worktree. PostgreSQL 16 is resolved as the canonical integration target; the implementation must update the test entry point before claiming database evidence. `E1-DEC-001` and `E1-DEC-002` are resolved.
  - Outcome: 01. Sanctum stateful session boundary works for the approved browser origins without bearer tokens. | 02. Registration creates at most one canonical User atomically, hashes the password once through the approved Laravel mechanism, and leaves no partial User/session state on failure. | 03. Register and current-account HTTP operations match every applicable v1 fixture row and never serialize credentials. | 04. Abuse/privacy controls enforce approved keys/windows/outcomes and safe `429` behavior without sensitive logs or duplicate disclosure. | 05. Backend evidence proves the contract, transaction, race, session, CSRF, throttling, and redaction behavior against the declared database.
  - Acceptance: 01. `GET /sanctum/csrf-cookie`, registration, session regeneration, and `GET /api/v1/auth/me` work with credentialed cookies and no browser bearer token. | 02. Name/email normalization, password 12–72 policy, unique/concurrent duplicate handling, authenticated-caller rejection, and transaction rollback match the fixture. | 03. Every `/api/v1` failure is JSON with the approved status/code/details shape; Public User is exactly `id/name/email`; current-account is private/no-store. | 04. Registration limiters use hashed trusted-IP/canonical-email keys, Redis windows, `Retry-After`, and safe logs. | 05. Focused backend tests pass on SQLite only where explicitly labeled fast/unit coverage, and integration/constraint/concurrency evidence passes against disposable PostgreSQL 16.
  - Verification: 01. `php artisan install:api` installed Sanctum 4.3.3; focused PostgreSQL integration tests cover CSRF bootstrap/expiry, session authentication, public projection, duplicate privacy, malformed JSON, bearer rejection, and throttling. | 02. `docker compose exec -T app php vendor/bin/phpunit -c phpunit.postgres.xml` passed with 12 tests and 55 assertions against PostgreSQL 16. | 03. `composer validate --no-check-publish` passed; `php artisan route:list --path=api/v1/auth` shows only the approved register/current-account routes; `git diff --check` passed. | 04. SQLite remains unsuitable for this repository's PostgreSQL-specific migration and is not used as integration evidence. | 05. Fixture corpus remains the canonical planning contract; frontend journey and broader concurrent-process evidence remain outside this backend implementation run.

- [x] TASK-1-1-03: Deliver and verify registration journey
  - Status: `done`
  - Owner: `Codex`
  - Branch/worktree: `feat/registration-journey-task-1-1-03`
  - Depends on: `TASK-1-1-01`
  - Covers: `AC-1-1-register-an-account-01`, `AC-1-1-register-an-account-02`, `AC-1-1-register-an-account-03`, `AC-1-1-register-an-account-04`, `AC-1-1-register-an-account-05`
  - Scope: 01. Implement frontend auth transport and registration adapter: shared API client and auth API/schema/error mapping | 02. Build registration state and accessible page: auth mutation/state, form, route and error presentation | 03. Verify registration components: registration component/state tests | 04. Verify registration end to end: registration/current-account browser scenarios
  - Coordination: `E1-COORD-AUTH-001`, `E1-COORD-TEST-001`
  - Blocked by: approved `TASK-1-1-01` fixture checkpoint, `E1-DEC-008` frontend verification enablement, and the backend acceptance gate from `TASK-1-1-02`; `E1-DEC-001`, `E1-DEC-002`, and `E1-DEC-007` are resolved.
  - Outcome: 01. Implemented cookie-session frontend transport, CSRF bootstrap/recovery, API error mapping, and public-user response handling. | 02. Completed cookie-backed auth state, protected-route current-account hydration, password-policy validation, accessible field errors, throttle/session messaging, and lost-response reconciliation. | 03. Added Vitest transport/schema/accessibility coverage. | 04. Added and passed Playwright registration-page smoke coverage; backend PostgreSQL suite passed as the cross-layer API gate.
  - Acceptance: 01. Implement frontend auth transport and registration adapter: browser sends only approved fields/credentials and maps every fixture. | 02. Build registration state and accessible page: keyboard-usable flow handles pending, validation, throttle, lost success and navigation safely. | 03. Verify registration components: known-bad UI/error/retry/accessibility states are pinned by tests. | 04. Verify registration end to end: happy path and meaningful failures pass without test-order dependence. | Integrated journey acceptance closes only after `TASK-1-1-02` is done with backend evidence.
  - Verification: 01. `npm run type-check`, `npm run lint`, `npm run build`, and `npm run test:unit -- --run` passed in `apps/web`; 6 unit/component tests passed. | 02. `npm run test:e2e` passed in `apps/web` with Chromium; registration page and accessible fields rendered through the Vite runtime. | 03. `docker compose exec -T app composer test` passed against PostgreSQL 16 with 15 tests and 61 assertions. | 04. `curl http://127.0.0.1:8080/api/health` passed; API nginx was available for local cross-layer runtime. | Full browser happy-path account creation was not claimed because the current browser smoke does not create persistent test data; backend happy-path and contract evidence remain in TASK-1-1-02.

## Dependency and concurrency map
- `TASK-1-1-01` depends on `none`.
- `TASK-1-1-02` depends on `TASK-1-1-01`; auth/session configuration and User identity/persistence can proceed independently after the shared fixtures, followed by integrated API/privacy controls and PostgreSQL 16 backend verification.
- `TASK-1-1-03` depends on `TASK-1-1-01`; its frontend or evidence work can proceed alongside `TASK-1-1-02`, and integrated acceptance closes after `TASK-1-1-02` is done.


Shared auth and test files remain reserved by the Epic coordination records.

## Coordination and verification gate
Use `E1-COORD-AUTH-001` for every shared auth/User file and `E1-COORD-TEST-001` for reusable test tooling. Task group 03 is the acceptance gate and requires evidence for every canonical AC; local contract variants are rejected.
